name: cassie-deployment
runbooks:
  - name: Deploy Cassie
    description: Deploys the cassie Anchor program
environments:
  localnet:
    inputs:
      network_id: localnet
      rpc_api_url: http://localhost:8899
    runbooks:
      - signers.localnet.tx
  devnet:
    runbooks:
      - signers.devnet.tx
  mainnet:
    runbooks:
      - signers.mainnet.tx